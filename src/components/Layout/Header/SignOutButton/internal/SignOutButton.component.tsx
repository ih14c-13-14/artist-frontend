import { COLOR } from '@/constants/COLOR';
import { Icon } from '@/features/ui/Icon';

const SignOutButton = () => {
  return <Icon type="SignOut" fill={COLOR.PRIMARY} />;
};

export default SignOutButton;
