import { Size } from '@/utils/size';

export const TOOLBAR_HEIGHT: Record<Size, number> = {
  SMALL: 48,
  MEDIUM: 64,
  LARGE: 80,
};

export const GUTTER = 24;

export const TOOLBAR_DEFAULT_HEIGHT = TOOLBAR_HEIGHT.SMALL;

export const TOOLBAR_DEFAULT_SPACING = 2;
